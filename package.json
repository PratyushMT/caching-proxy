{
  "name": "caching-proxy",
  "version": "1.0.0",
  "description": "CLI-based HTTP caching proxy server.",
  "main": "src/index.js",
  "bin": {
    "caching-proxy": "./src/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "commander": "^14.0.0",
    "express": "^5.1.0"
  }
}
